<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Internal app files -->
    <files-path name="internal_files" path="." />
    <files-path name="internal_recordings" path="Download/" />

    <!-- External app-specific files -->
    <external-files-path name="pictures" path="Pictures/" />
    <external-files-path name="download" path="Download/" />
    <external-files-path name="documents" path="Documents/" />
    <external-files-path name="files" path="." />

    <!-- Cache directory -->
    <cache-path name="cache" path="." />

    <!-- APK -->
    <external-files-path
        name="apk_folder"
        path="ApkFolder/" />

    <!-- 🔥 IMPORTANT: Add external public storage access -->
    <external-path name="public_download" path="Download/" />
    <external-path name="public_documents" path="Documents/" />
</paths>
